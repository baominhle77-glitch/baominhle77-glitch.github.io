import assert from "node:assert/strict";
import fs from "node:fs";

const gate = fs.readFileSync(new URL("./gate.js", import.meta.url), "utf8");
const gateCss = fs.readFileSync(new URL("./gate.css", import.meta.url), "utf8");
const community = fs.readFileSync(new URL("./community.js", import.meta.url), "utf8");
const admin = fs.readFileSync(new URL("./community-admin.js", import.meta.url), "utf8");
const adminHtml = fs.readFileSync(new URL("../boitoan/community-admin.html", import.meta.url), "utf8");
const e2eWorkflow = fs.readFileSync(new URL("../.github/workflows/e2e-reader-production.yml", import.meta.url), "utf8");
const webkitCheck = fs.readFileSync(new URL("../tools/webkit-production-check.mjs", import.meta.url), "utf8");

assert.match(gate, /class="gate-entry-choice"/, "màn đầu phải chỉ có các lựa chọn");
assert.match(gate, /data-entry-stage hidden/, "màn thao tác phải ẩn trước khi chọn");
assert.match(gate, /data-entry-open="login"[\s\S]*data-entry-open="register"[\s\S]*data-entry-open="admin"/);
assert.match(gate, /if \(!payload\) \{[\s\S]*reveal\(method \|\| "member"\)/, "trang plaintext không được ép giải mã sau đăng ký");
assert.match(gate, /community_profile_boitoan/, "phải lưu hồ sơ để hiện badge vai trò trong app");
assert.match(gate, /Account V5 single admin login/, "Admin phải dùng một luồng đăng nhập server duy nhất");
assert.match(gate, /\/api\/community\/admin\/login/, "cửa đầu phải lấy JWT Admin từ Worker");
assert.match(gate, /market_admin_token/, "JWT Admin phải được lưu cho trang Quản trị");
assert.match(gate, /location\.replace\("\.\/community-admin\.html"\)/, "đăng nhập Admin thành công phải vào thẳng Quản trị");
assert.doesNotMatch(gate, /unlockLocalOrEncrypted\(pass, form\.remember\.checked\)[\s\S]{0,500}claimPrimaryAdminDevice\(pass\)/, "không được kiểm mật khẩu local rồi dùng lại như ADMIN_TOKEN");
assert.match(gate, /function marketAdminSession\(/, "phải nhận diện phiên Admin trên app chính");
assert.match(gate, /community-admin\.html/, "badge và bottom navigation Admin phải mở trang quản trị");
assert.match(gate, /Mở quản trị/, "giao diện Admin phải có chỉ dẫn quản trị rõ ràng");
assert.match(gate, /targetLabel = admin \? "Quản trị" : "Cộng đồng"/, "bottom navigation phải đổi đúng nhãn theo phiên");
assert.match(gate, /Account V3 iOS mutation guard/, "phải có chốt chống vòng lặp MutationObserver trên WebKit");
assert.match(gate, /if \(labelNode\.textContent !== targetLabel\) labelNode\.textContent = targetLabel/, "chỉ được thay text khi nội dung thực sự đổi");
assert.doesNotMatch(gate, /link\.querySelector\("\.market-nav-label"\)\.textContent\s*=/, "không được gán textContent vô điều kiện trong injectCommunity");
assert.match(gateCss, /input\[type="checkbox"\][\s\S]*width:auto !important/, "checkbox không được kéo thành ô dài trên iPhone");
assert.match(gateCss, /a\.market-account-identity/, "badge Admin có thể bấm trên thiết bị cảm ứng");
assert.match(community, /function renderPosts\(/, "member phải có khu thảo luận chung");
assert.match(community, /community-role-badge role-/, "vai trò phải hiện cạnh tên");
assert.match(community, /requestedView === "profile"\) renderProfile\(\)/, "Admin tổng phải vào thẳng Trang cá nhân");
assert.match(community, /tokenClaims\(\)\.mode === "impersonation"/);
assert.match(community, /Chế độ chỉ đọc/);
assert.match(community, /Quay lại khu vực Admin/);

assert.match(admin, /market_admin_token/, "trang Quản trị phải dùng JWT đã cấp ở cửa đầu");
assert.match(admin, /activateTab\("users"\)/, "trang Quản trị phải tự tải dữ liệu");
assert.match(admin, /community-admin\/session/, "đăng xuất phải thu hồi phiên server");
assert.match(admin, /Xóa tài khoản/);
assert.match(admin, /Xem trang cá nhân/);
assert.match(admin, /admin_view=profile/);
assert.doesNotMatch(admin, /community-admin-token|Mật khẩu Admin|Bearer " \+ tokenInput/, "trang Quản trị không được hỏi hoặc đọc mật khẩu lần hai");
assert.match(admin, /Mở bài thảo luận/);
assert.match(adminHtml, /data-admin-tab="posts"/);
assert.match(adminHtml, /Admin tổng/);
assert.match(adminHtml, /community-admin-logout/);
assert.doesNotMatch(adminHtml, /type="password"|Mật khẩu Admin|Đặt thiết bị này làm Admin tổng/, "HTML Quản trị không được có form mật khẩu hoặc bind thủ công");

assert.match(e2eWorkflow, /workflow_run:/, "E2E phải chạy sau deploy production");
assert.match(e2eWorkflow, /Reader production verification/, "E2E chỉ chạy cho commit nghiệm thu được đánh dấu");
assert.match(e2eWorkflow, /role:'reader'/, "E2E phải tạo tài khoản Reader thật");
assert.match(e2eWorkflow, /E2E iPad Safari/, "E2E phải đăng nhập trên nền tảng thứ hai");
assert.match(e2eWorkflow, /playwright@1\.52\.0/, "E2E phải cài Playwright cố định phiên bản");
assert.match(e2eWorkflow, /node --check tools\/webkit-production-check\.mjs/, "workflow phải syntax-check script WebKit trước khi chạy");
assert.match(e2eWorkflow, /node tools\/webkit-production-check\.mjs/, "workflow phải chạy checker độc lập");
assert.match(e2eWorkflow, /-X DELETE[\s\S]*\/api\/community\/me/, "E2E phải xóa tài khoản qua API đã xác thực");
assert.match(e2eWorkflow, /token_not_revoked/);
assert.match(e2eWorkflow, /account_still_loginable/);
assert.match(e2eWorkflow, /READER_E2E_STATUS\.md/);

assert.match(webkitCheck, /webkit\.launch/, "checker phải dùng engine WebKit thực tế");
assert.match(webkitCheck, /locked_page_crashed/, "checker phải phát hiện trang production sạch bị crash");
assert.match(webkitCheck, /fixture_page_crashed/, "checker phải phát hiện fixture gate bị crash");
assert.match(webkitCheck, /route\.fulfill/, "fixture phải cùng origin và nạp đúng asset production");
assert.match(webkitCheck, /\/assets\/gate\.js\?webkit=/, "fixture phải tải gate.js production");
assert.match(webkitCheck, /fixture_mutation_loop/, "checker phải phát hiện vòng lặp DOM mutation");
assert.match(webkitCheck, /mutationDelta/, "checker phải đo mutation có ổn định sau khi render hay không");
assert.match(webkitCheck, /#gate-community-link/, "checker phải xác nhận chỉ có một lối Cộng đồng\/Quản trị");
assert.doesNotMatch(webkitCheck, /!state\.key|!login\.key/, "checker không được phụ thuộc khóa giải mã bí mật");
assert.match(webkitCheck, /WEBKIT_E2E_ERROR/, "checker phải ghi lỗi WebKit cụ thể để chẩn đoán");
assert.match(webkitCheck, /production_gate_missing_ios_guard/, "checker phải xác nhận production đang phục vụ đúng asset có guard");

console.log("Account V5 single Admin login, Account V3 iOS guard and production E2E contracts PASS");
